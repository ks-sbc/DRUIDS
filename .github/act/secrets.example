# Example secrets file for Act local testing
# Copy this to .secrets and fill in real values for local testing
# DO NOT commit .secrets file - it should be in .gitignore

# GitHub token for deployment (not needed for local testing)
GITHUB_TOKEN=your_github_token_here

# Any other secrets your workflow might need
# Add them here in KEY=value format